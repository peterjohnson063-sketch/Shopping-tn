/* Safe to commit. Real key: copy gemini-key.local.example.js → gemini-key.local.js (gitignored). */
if (typeof window.EVEREST_GEMINI_API_KEY === 'undefined') {
  window.EVEREST_GEMINI_API_KEY = '';
}
